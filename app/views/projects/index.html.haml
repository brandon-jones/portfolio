%div.container-fluid
  = render :partial => 'layouts/title', locals: { title: "Brandon's Projects" }
  %div.row
    %div.col-md-6.col-md-offset-3
      -@projects.reverse.each do |project|
        = render :partial => 'project_post', locals: { title: true, project: project }
        %hr